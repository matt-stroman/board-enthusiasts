name: Root Validation

on:
  push:
    branches:
      - main
      - feat/cloudflare-supabase-workers-conversion
  pull_request:
    branches:
      - main
      - feat/cloudflare-supabase-workers-conversion

permissions:
  contents: read

concurrency:
  group: root-validation-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    name: Verify Maintained Stack
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Show submodule status
        run: git submodule status --recursive

      - name: Run maintained verification
        run: python ./scripts/dev.py verify --skip-contract-tests

  contract-smoke:
    name: Contract Smoke
    runs-on: ubuntu-latest
    needs: verify

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Run contract smoke tests
        run: python ./scripts/dev.py contract-smoke --start-workers

  staging-dry-run:
    name: Staging Dry Run
    runs-on: ubuntu-latest
    needs: verify

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Validate staging deployment flow
        run: python ./scripts/dev.py deploy-staging --dry-run
